version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gocritic
    - gosec
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
